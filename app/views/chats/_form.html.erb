<%= simple_form_for @chat, id: 'new_chat' do |f| %>
	<%= f.error_notification %>
  	<%= f.input :name, label: "Chat name", placeholder: "Enter chat name" %>
	<%= f.hidden_field :identifier, value: SecureRandom.hex %>
	<%= f.submit %>
<% end %>